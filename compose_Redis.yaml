version: "3.9"
services:
  redis:
    image: redis:latest
    container_name: redis(site de estudos)
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    restart: always

volumes:
  redis_data:
